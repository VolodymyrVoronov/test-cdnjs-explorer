export const PATHS = {
  START: "/",
  MAIN: "/main",
} as const;

export const ROUTES = {
  START: PATHS.START,
  MAIN: PATHS.MAIN,
} as const;

export const URL = "https://api.cdnjs.com/libraries/";
export const REQUEST_ERROR = 404;
